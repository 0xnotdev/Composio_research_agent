# Required for live research. Keep real values in .env; never commit that file.
COMPOSIO_API_KEY=
OPENROUTER_API_KEY=
# Select and preflight a currently available free model before the 100-app run.
OPENROUTER_MODEL=
# Optional: use only after inspecting the live Composio tool schema.
COMPOSIO_API_BASE=https://backend.composio.dev/api/v3.1
